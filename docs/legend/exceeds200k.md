# `200k+`

The last response went past 200,000 tokens of context (Claude Code's `exceeds_200k_tokens`
flag). Informational rather than a warning, so it is dim.

It is shown because long-context use can be accounted for separately in plan usage; `/usage`
has the details.

---

[← All segments](./index.md)
